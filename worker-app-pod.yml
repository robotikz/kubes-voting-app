apiVersion: v1
kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: workder-app-pod
    app: demo-voting-app
spec:
  containers:
  - name: voting-app
    image: kodekloud/examplevotingapp_worker:v1
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
